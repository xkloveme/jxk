/**
 * 判断是否是Null值
 * @author xkloveme <xkloveme@gmail.com>
 * @category is
 * @alias is_null
 * @param {any} value 任意值
 * @returns {Boolean} 返回是否是Null值
 */
export default (value) => {
  return value === null;
};
