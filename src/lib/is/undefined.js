/**
 * 判断是否是Undefined
 * @author xkloveme <xkloveme@gmail.com>
 * @category is
 * @alias is_undefined
 * @param {any} value 任意值
 * @returns {Boolean} 返回是否是Undefined值
 */
export default (value) => {
  return value === undefined;
};
